{
  "name": "order-summary-component",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "parcel build src/index.html",
    "deploy": "npm run build && surge dist/ --domain bgrapes-order-summary.surge.sh",
    "develop": "parcel src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.3.0"
  }
}
